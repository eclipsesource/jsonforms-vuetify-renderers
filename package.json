{
  "name": "jsonforms-vuetify-renderers",
  "private": true,
  "scripts": {
    "build": "lerna run --scope '@jsonforms/vue-vuetify' build",
    "watch": "lerna run --scope '@jsonforms/vue-vuetify' watch",
    "test": "lerna run test",
    "clean": "lerna run clean",
    "example:dev": "lerna run --scope 'example' dev",
    "example:build": "lerna run --scope 'example' build"
  },
  "devDependencies": {
    "lerna": "8.1.2"
  },
  "engines": {
    "node": "^18.19.0",
    "pnpm": ">=7.13.4"
  }
}
