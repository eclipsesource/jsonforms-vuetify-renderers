import { JsonExampleInput } from '@/core/types';
import data from './data.json';
import schema from './schema.json';

const uischema = undefined;
export const input: JsonExampleInput = { schema, uischema, data };
